<h1>Seu livro resgatado:</h1>

<div class="books-container">
    <div>
      #{{book.id}} - <span class="bold">{{book.title}}</span> - {{book.n_pages}} páginas
    </div>
    <form action="/books/remove/{{book.id}}" method="POST">
      <input type="submit" value="Remover">
    </form>
</div>